user:
  name: Incredaimaso
  github: Incredaimaso

language: "python"
python_version: "3.13"

commands:
  build:
    - "pip install uv"
    - "uv pip install -e . --system"
    - "which tgmusic"
    
  start:
    - "tgmusic"

env:
  - "ENVIRONMENT=production"

