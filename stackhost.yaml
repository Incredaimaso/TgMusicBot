user:
  name: ishikki
  github: ishikki-akabane

language: "python"
python_version: "3.13"

commands:
  build:
    - "pip install -e . --system"

  package:
    - "nano ffmpeg git"
  start:
    - "tgmusic"

env:
  - "ENVIRONMENT=production"
